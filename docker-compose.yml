version: '3.8'

services:
  # PostgreSQL local - commenté car vous utilisez pgAdmin local
  # postgres:
  #   image: postgres:15-alpine
  #   container_name: cocoa_db
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: kidjamo@
  #     POSTGRES_DB: cocoa_db
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

  backend:
    build: ./backend
    container_name: cocoa_backend
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://postgres:kidjamo%40@host.docker.internal:5432/cocoa_db
      JWT_SECRET: your-super-secret-jwt-key-change-this-in-production
      JWT_ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 30
      REFRESH_TOKEN_EXPIRE_DAYS: 7
    # depends_on:
    #   postgres:
    #     condition: service_healthy
    volumes:
      - ./backend:/app
    network_mode: bridge

# volumes:
#   postgres_data:
